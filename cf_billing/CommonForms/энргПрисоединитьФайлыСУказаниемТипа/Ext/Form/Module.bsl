#Область СобытияФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ОбщегоНазначенияКлиентСервер.ЗаполнитьКоллекциюСвойств(Параметры.ТаблицаСДанными, ФайлыСУказаниемТипа);
	
	Если Параметры.Свойство("ВладелецФайлов") Тогда 
		ВладелецФайла = Параметры.ВладелецФайлов; 
		энргОбщегоНазначения.УстановитьДоступностьЭлементовПоРолям(ЭтотОбъект, Новый Структура("ФайлыСУказаниемТипа"), ВладелецФайла.Метаданные(), "Редактирование");
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область СобытияРеквизитовФормы

&НаКлиенте
Процедура ДанныеФайловПутьКФайлуНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	ТекДанные = Элементы.ФайлыСУказаниемТипа.ТекущиеДанные;
	
	ДиалогВыбораФайла 						= Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
	ДиалогВыбораФайла.МножественныйВыбор 	= Ложь;
	ДиалогВыбораФайла.Заголовок 			= НСтр("ru = 'Выбор файла'");
	Если НЕ ДиалогВыбораФайла.Выбрать() Тогда
		Возврат;
	КонецЕсли; 
	ТекДанные.ПутьКФайлу = ДиалогВыбораФайла.ПолноеИмяФайла;
КонецПроцедуры

#КонецОбласти

#Область ВозвратРезультата

&НаКлиенте
Процедура Отмена(Команда)
	Закрыть(Неопределено);
КонецПроцедуры

&НаКлиенте
Процедура ОК(Команда)
	РезультатПроверки = ПроверитьСодержаниеНомераИДаты();
	ОчиститьСообщения();
	Если НЕ РезультатПроверки.Количество() = 0 Тогда
		Для Каждого ТекСтрока из РезультатПроверки Цикл
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю("В строке " + (ТекСтрока.ИндексСтроки + 1) + " необходимо заполнить номер и дату");
		КонецЦикла;
		
		Возврат;
	КонецЕсли;
	
	Закрыть(ПолучитьРезультат());
КонецПроцедуры

&НаСервере
Функция ПроверитьСодержаниеНомераИДаты()
	Возврат энргРаботаСПрикрепленнымиФайламиСервер.ПроверитьСодержаниеНомераИДаты(ЭтотОбъект, "ФайлыСУказаниемТипа");
КонецФункции

&НаСервере
Функция ПолучитьРезультат()
	Возврат ОбщегоНазначения.ТаблицаЗначенийВМассив(ФайлыСУказаниемТипа.Выгрузить());
КонецФункции

#КонецОбласти



