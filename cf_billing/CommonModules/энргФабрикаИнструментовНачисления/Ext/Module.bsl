
#Область СтандартныеПроцедуры

Процедура Инициализаровать() Экспорт 
	ИнструментыНачислений();
	ИнструментыНачисленийМКД();
	РаботаСПериодомДействия();
	РаботаСПоследовательностями();
	РаботаСПоследовательностямиСобытия();
	ПроведениеПоСтабильнымПериодам();
	СервисПроведенияПоСтабильнымПериодам();
КонецПроцедуры

Функция Серия() Экспорт 
	Возврат "6585";	
КонецФункции

Функция ИнструментыНачислений() Экспорт	
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргИнструментыНачислений");	
	Иначе
		ИнструментОбъект = Обработки.энргИнструментыНачислений.Создать();	
	КонецЕсли;
	
	Возврат ИнструментОбъект;
	
КонецФункции

Функция ИнструментыНачисленийПени() Экспорт	
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргИнструментыНачисленийПени");		
	Иначе
		ИнструментОбъект = Обработки.энргИнструментыНачисленийПени.Создать();
	КонецЕсли;
		
	Возврат ИнструментОбъект;
	
КонецФункции


Функция ИнструментыНачисленийМКД() Экспорт	
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргИнструментыНачисленийМКД");
	Иначе
		ИнструментОбъект = Обработки.энргИнструментыНачисленийМКД.Создать();	
	КонецЕсли;
	
	Возврат ИнструментОбъект;
	
КонецФункции

Функция ИнструментыПерерасчет() Экспорт 
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргИнструментыПерерасчет");
	Иначе
		ИнструментОбъект = Обработки.энргИнструментыПерерасчет.Создать();	
	КонецЕсли;

	Возврат ИнструментОбъект;
	
КонецФункции
	
Функция РаботаСПериодомДействия() Экспорт 	
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргРаботаСПериодомДействия");
	Иначе
		ИнструментОбъект = Обработки.энргРаботаСПериодомДействия.Создать();	
	КонецЕсли;

	Возврат ИнструментОбъект;
КонецФункции

Функция РаботаСПоследовательностями() Экспорт
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргРаботаСПоследовательностями");	
	Иначе
		ИнструментОбъект = Обработки.энргРаботаСПоследовательностями.Создать();	
	КонецЕсли;

	Возврат ИнструментОбъект; 
	
КонецФункции
	
Функция РаботаСПоследовательностямиСобытия() Экспорт   	
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргРаботаСПоследовательностямиСобытия");
	Иначе
		ИнструментОбъект = Обработки.энргРаботаСПоследовательностямиСобытия.Создать();	
	КонецЕсли;
		
	Возврат ИнструментОбъект; 
	
КонецФункции

Функция ПроведениеПоСтабильнымПериодам() Экспорт 
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргПроведениеПоСтабильнымПериодам");
	Иначе
		ИнструментОбъект = Обработки.энргПроведениеПоСтабильнымПериодам.Создать();	
	КонецЕсли;
	
	Возврат ИнструментОбъект;
	
КонецФункции

Функция СервисПроведенияПоСтабильнымПериодам() Экспорт 
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда 
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргСервисПроведенияПоСтабильнымПериодам");
	Иначе
		ИнструментОбъект = Обработки.энргСервисПроведенияПоСтабильнымПериодам.Создать();			
	КонецЕсли;

	Возврат ИнструментОбъект;  
	
КонецФункции

Функция СервисПроведенияПоСтабильнымПериодамПерерасчет() Экспорт 
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргСервисПроведенияПоСтабильнымПериодамПерерасчет");	
	Иначе
		ИнструментОбъект = Обработки.энргСервисПроведенияПоСтабильнымПериодамПерерасчет.Создать();	
	КонецЕсли;
	
	Возврат ИнструментОбъект;  
	
КонецФункции

Функция ДелегатНачислений() Экспорт 
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда 
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргДелегатНачислений");	
	Иначе
		ИнструментОбъект = Обработки.энргДелегатНачислений.Создать(); 	
	КонецЕсли;
	
	Возврат ИнструментОбъект;
	
КонецФункции

Функция ДелегатПерерасчета() Экспорт 
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргДелегатПерерасчет");
	Иначе
		ИнструментОбъект = Обработки.энргДелегатПерерасчет.Создать();	
	КонецЕсли;

	Возврат ИнструментОбъект; 
	
КонецФункции

Функция СервисВыполненияОтложенныхДвижений() Экспорт
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргСервисОтложенныеДвиженияДокументов");
	Иначе
		ИнструментОбъект = Обработки.энргСервисОтложенныеДвиженияДокументов.Создать();	
	КонецЕсли;
		
	Возврат ИнструментОбъект;
	
КонецФункции

Функция СервисВосстановленияПоследовательностиПоказанийПУ() Экспорт
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргВосстановлениеПоследовательностиПоказаний");
	Иначе
		//ИнструментОбъект = Обработки.энргСервисВосстановленияПоследовательностиПоказаний.Создать();
		ИнструментОбъект = Обработки.энргВосстановлениеПоследовательностиПоказаний.Создать(); 	
	КонецЕсли;

	Возврат ИнструментОбъект;
	
КонецФункции

Функция СервисУстановкиЗначенийДифференцированныхТарифов() Экспорт
	
	ИспользоватьПодсистемуСЛК = ИспользоватьПодсистемуСЛК();
	
	Если ИспользоватьПодсистемуСЛК Тогда
		ИнструментОбъект = слкМенеджерЗащиты.СоздатьОбъект(Серия(), "слкОбъектыСЛК.энргСервисУстановкиЗначенийДифференцированныхТарифов");
	Иначе
		
		ИнструментОбъект = Обработки.энргСервисУстановкиЗначенийДифференцированныхТарифов.Создать();
		
		//ИмяEpf = "\\172.29.1.50\Users\Xabarov\РКС\Обработки и отчеты\Обработки\энргСервисУстановкиЗначенийДифференцированныхТарифов.epf";
		//ИнструментОбъект = ВнешниеОбработки.Создать(ИмяEpf, Ложь);
		
			
	КонецЕсли;
		
	Возврат ИнструментОбъект;
	
КонецФункции

#КонецОбласти  

#Область СЛК  

Функция ИспользоватьПодсистемуСЛК()
	Возврат Ложь;//Истина;	
КонецФункции

#КонецОбласти
