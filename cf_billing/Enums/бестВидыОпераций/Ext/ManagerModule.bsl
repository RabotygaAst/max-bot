#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции	

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	Отбор  = Параметры.отбор;
	Если Отбор.свойство("Ссылка") и ТипЗнч(Отбор.Ссылка) <> Тип("ФиксированныйМассив") тогда
		ТипДокумента  				= Отбор.ссылка;
		СтандартнаяОбработка 		= Ложь;
		ДанныеВыбора 				= Новый СписокЗначений;
		МассивЗначений  			= Новый Массив;
		бестРаботаСВидамиОперацийСервер.ЗаполнитьСписокВозможныхОперацийФормаСписка(ТипДокумента,ДанныеВыбора);
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#КонецЕсли
